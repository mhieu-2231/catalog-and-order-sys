@{
    // Layout mặc định cho Client. Trong controller/action của Admin,
    // set ViewData["Layout"] = "_AdminLayout" hoặc gán Layout trực tiếp
    // trong từng view Admin (Layout = "_AdminLayout";)
    Layout = "_Layout";
}
